// const object = { a: 1, b: 2, c: 3 };

// for (const property in object) {
//   console.log(`${property}: ${object[property]}`);
// }


const object = {
    foo: "Cortlyn",
    bar: "Weston",
    fum: "Marleigh",
    quux: "Nixon",
    spam: "Sterling",
}

for (const property in object){
    console.log(`${property}: ${object[property]}`);
}